<UserControl x:Class="DocEditor.DictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="150">
    
    <Grid Background="#2e2f34" Width="150">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="10" BorderBrush="#404146" BorderThickness="2" CornerRadius="5">
            <TextBlock Height="350" Background="#404146"> Dictionary</TextBlock>
        </Border>

        <Grid Grid.Row="1" Background="#2e2f34" Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="EDIT" Style="{StaticResource DictBtnStyle}"  />
            <Button Grid.Column="1" Content="REMOVE" Style="{StaticResource DictBtnStyle}"  />
        </Grid>
    </Grid>
</UserControl>
